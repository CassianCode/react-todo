const gap = 4;

const styling = {
    colors: {
        neutral: "#f5f7f7",
        primary: "#f5f7f7",
    },
    gaps: {
        gap: `${gap}rem`,
        halfGap: `${gap / 2}rem`,
    },
}

export default styling;